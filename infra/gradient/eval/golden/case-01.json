{
  "test_case_id": "case-01",
  "description": "Transaction categorization classification test",
  "input": {
    "endpoint": "GET /api/transactions",
    "fields": ["amount", "merchant", "category", "date"],
    "code_snippet": "const transactions = await db.transactions.findMany({ where: { userId }, select: { amount: true, merchant: true, category: true, date: true } });"
  },
  "expected_output": {
    "purpose": "budgeting",
    "data_category": "financial_transaction",
    "confidence": 0.9,
    "reasoning": "Transaction data with merchant and category fields indicates budgeting and spending analysis use case"
  },
  "tolerance": {
    "confidence_min": 0.8,
    "purpose_exact": true,
    "data_category_exact": true
  }
}
